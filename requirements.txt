# Intentionally (almost) empty.
#
# llama-cpp-python is NOT listed here on purpose: a plain
# `pip install llama-cpp-python` produces a CPU-only build. The node pack
# installs a matching CUDA build by itself on first use (see
# llama_bootstrap.py) or via install.py when installed through
# ComfyUI-Manager.
#
# numpy, Pillow and torch already ship with ComfyUI.
