# llama-cpp-python is intentionally NOT listed here.
#
# Installing it via plain `pip install llama-cpp-python` gives you a
# CPU-only build. For GPU inference you must install a CUDA build
# yourself - see README.md ("Installing llama-cpp-python").
#
# Everything else this node pack needs (numpy, Pillow, torch) already
# ships with ComfyUI.
