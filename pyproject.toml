[project]
name = "comfyui-localllm-nodes"
description = "Local GGUF LLM/VLM nodes for ComfyUI (llama.cpp): prompt generation, image captioning, prompt library, model catalog with auto-download and self-installing CUDA backend."
version = "2.0.0"
license = { text = "MIT" }
dependencies = []

[project.urls]
Repository = "https://github.com/Slartibart23/ComfyUI-LocalLLM-Nodes-v1.2.0"

[tool.comfy]
PublisherId = "slartibart23"
DisplayName = "ComfyUI-LocalLLM-Nodes"
